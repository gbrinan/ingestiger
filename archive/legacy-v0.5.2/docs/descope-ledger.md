# 디스코프 원장 (negatives-as-corpus)

> 위험 인접이라 실행 범위에서 도려낸 항목의 기록. 지우지 않는다 — 나중 패스가 여기서
> 안티패턴을 캐낼 수 있어야 한다. 각 항목: 클래스 · 판정 · 사유 · 안전 대안.

## 2026-08-31 카브 (세션 6)

대상 작업: soloforce2 런타임 검증, 미러 검증 기계화, 호스트 커밋·라우팅 등록,
한국형 PII NER 설치·시험, STT 신뢰도 캘리브레이션.

| # | 항목 | 클래스 | 판정 | 사유 | 안전 대안 |
|---|---|---|---|---|---|
| C1 | PII 탐지 성능 시험에 **실제 개인정보** 표본 사용 | 브라이트라인 | descoped | 안전한 버전이 없다. 탐지율을 재려고 진짜 주민번호·계좌를 들이는 순간 그 데이터가 로그·픽스처·이력에 남는다 | 형식만 유효한 합성 값으로 시험 — v0.5.0에서 이 방식이 실제 결함(한글 인접 미탐)을 잡아 실효성이 이미 입증됨 |
| C2 | 다운로드 폴더 **전수 스캔**으로 배치 확장 | 그레이존 | descoped | 지시 없는 폴더·제3자 문서까지 열람하게 된다. 스킬 자신의 규칙("지시 없는 폴더는 열지 않는다") 위반 | 소스를 사용자가 명시한 경로로 한정하고 `manifest.py`가 그 경로를 고정 |
| C3 | 민감 정산 파일을 **PII 회귀 표본으로 재사용** | 그레이존 | descoped | 얻을 것(탐지 확인)은 합성 표본으로 이미 얻었고, 잃을 것(민감 데이터 반복 열람)만 남는다 | 구조만 모사한 합성 표본으로 회귀. 원본은 건너뜀 상태 유지, 경로만 기록 |
| C4 | 드라이브 미러 **범위 확대** | 그레이존 | descoped | 클라우드에는 코퍼스 밖 공유·타인 파일이 함께 있다. 대상·공유범위를 사용자가 정하지 않은 확장 | 현 9건 미러 유지. `mirrorcheck.py`는 **대조만** 하고 자동 덮어쓰기·삭제 기능을 갖지 않는다 |
| C5 | soloforce2 **커밋·푸시** | 그레이존 | descoped (사유는 위험이 아니라 순서) | 검증 전에 이력을 남기는 것은 순서가 거꾸로다. 독립 재스윕도 "커밋 행위 자체는 안전, 위험한 것은 커밋 내용"이라고 판정했고 이에 동의한다 | 로컬 복사까지만. 이번 검증으로 정합성이 확인됐으므로 다음 커밋은 근거 있는 행동이 된다 |

### 독립 재스윕(N=1)이 내 카브에서 찾아낸 공백

| 지적 | 처리 |
|---|---|
| **제외 목록 자체가 유출 벡터** — 민감 파일명·거래처명을 규칙에 리터럴로 적었다면 커밋이 영구 각인 | 실측: 리포 작업트리·git 전체 이력·정본 폴더·호스트 복사본 전부 0건. 기록 수준은 파일명과 사유 범주뿐 |
| **매니페스트·미러 리포트가 파일명을 남긴다** — 내용이 아니어도 파일명 자체가 정보 | 잔여로 인정. 드라이브 미러의 `index.md`에 민감 파일의 **파일명과 "민감" 사유**가 존재(실명·값은 없음). 다음 미러 갱신 때 판단할 사안으로 남긴다 |
| **미러 체커가 "차이 해소"로 번질 위험** (자동 삭제·덮어쓰기는 브라이트라인 하위행위) | 카브가 이미 차단. `mirrorcheck.py`는 읽기·대조·보고만 하며 쓰기 경로가 없다 |
| **C5 과잉 배제** | 인정하되 유지. 배제 사유를 "위험"이 아니라 "순서"로 원장에 명시 |

### 이번 실행에서 안전 범위가 산출한 것

- soloforce2 의존 가이드 4종 실재 확인 — 스킬의 최대 미확인 전제 해소
- role-directive 참조 경로 불일치 발견·수정 (레포와 호스트의 경로 깊이 차이)
- `scripts/mirrorcheck.py` 신설 — 정합 통과·오염 실패 양방향 실증
- 게이트 봉인이 신규 스크립트를 검출함을 실증
